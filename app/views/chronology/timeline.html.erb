<h1>Cronologia Bíblia</h1>

<div class="timeline">
  <div class="line">
    <hr>
  </div>
  <div class="events">
    <% @timeline.each do |event| %>
      <% event = EventDecorator.new(event) %>
      <div class="event" style="<%= event.inline_css  %>" title="<%= event.name %>"><%= event.formatted_year  %> - <%= event.name %></div>
    <% end %>
  </div>
</div>
